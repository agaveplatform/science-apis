{
  "HTTP_PORT": 8085,
  "JWT_AUTH": false,
  "dbname": "${foundation.db.database}",
  "driver_class": "${foundation.db.driver}",
  "url": "${foundation.db.url}",
  "user": "${foundation.db.username}",
  "password": "${foundation.db.password}",
  "max_pool_size": 10
}